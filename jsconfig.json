{
  "compilerOptions": {
    "baseUrl": ".",             // project root
    "paths": {
      "@/*": ["app/*"]          // @ resolves to app/ folder
    }
  },
  "include": [
    "app/**/*.js",
    "app/**/*.jsx"
  ]
}